spring:
  profiles:
    active: prod
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/dbperson?characterEncoding=utf8&useSSL=true
    username: root
    password: root
    useSSL: false
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true